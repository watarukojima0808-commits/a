# ツール集

| ツール | 概要 |
|--------|------|
| [日経225 デイトレ銘柄スクリーナー](#日経225-デイトレ銘柄スクリーナー) | 毎朝の寄り付き前にデイトレ向き銘柄を抽出 |
| [エリオット3波入り候補スクリーナー](#エリオット3波入り候補スクリーナー) | 毎日の大引け後に「2波が終わり3波に入りそうな銘柄」を抽出 |
| [メルカリ 価格アラートボット](#メルカリ-価格アラートボット) | 指定キーワードの新着出品を監視 |
| [聖杯検証エンジン](#聖杯検証エンジン) | 手法の優位性が本物かまぐれかを統計的に判定 |
| [資金管理・発注数量計算](#資金管理発注数量計算) | 資金100万円で実際に何株買うかを決める |

いずれも Python 3.x の標準ライブラリのみで動作します（追加インストール不要）。

---

# 日経225 デイトレ銘柄スクリーナー

日経225構成銘柄の日足データから、当日のデイトレードに向いた銘柄をスコア順に抽出します。
寄り付き前（8:45頃）に実行し、前営業日終値までのデータで当日の候補を絞り込む想定です。

## セットアップ

初回のみ、構成銘柄リストを日経公式から取得してください（定期入れ替えがあるため、数ヶ月に一度は実行推奨）。

```bash
python nikkei_daytrade.py --update-list
```

リポジトリには `data/nikkei225.csv` にスナップショットが同梱されていますが、
入れ替えが反映されていない可能性があるため、上記の更新を先に実行するのが確実です。

## 使い方

```bash
# 上位10銘柄をターミナルに表示
python nikkei_daytrade.py

# 上位15銘柄、ATR2%以上に絞り、レポートを reports/ に保存
python nikkei_daytrade.py --top 15 --min-atr 2.0 --out-dir reports

# 結果をGmailで自分宛に送る
python nikkei_daytrade.py --out-dir reports \
  --gmail-from you@gmail.com --gmail-to you@gmail.com \
  --gmail-password 'xxxx xxxx xxxx xxxx'
```

### オプション

| オプション | デフォルト | 説明 |
|-----------|-----------|------|
| `--top` | 10 | 表示する上位銘柄数 |
| `--min-atr` | 1.5 | ATR(14)の下限（%）。値幅の出ない銘柄を除外 |
| `--min-turnover` | 20 | 20日平均売買代金の下限（億円）。板の薄い銘柄を除外 |
| `--min-price` | 500 | 株価の下限（円） |
| `--max-price` | 0 | 株価の上限（円、0で無制限） |
| `--out-dir` | なし | Markdownレポートの保存先。`YYYY-MM-DD.md` で出力 |
| `--workers` | 6 | 並列取得数 |
| `--codes-file` | `data/nikkei225.csv` | 銘柄リストCSV（`code,name` 形式） |
| `--update-list` | — | 構成銘柄リストを更新して終了 |

## スコアの内訳（100点満点）

デイトレードで利益機会があるかどうかを、4つの観点で採点しています。

| 配点 | 指標 | 考え方 |
|------|------|--------|
| 40点 | ATR(14) / 株価 | 値幅がなければ利益機会がない。4%以上で満点 |
| 25点 | 前日出来高 / 20日平均 | 出来高急増は当日も値動きが続くサイン。2.5倍で満点 |
| 20点 | 20日平均売買代金 | 板が厚いほど約定が滑らない。1日100億円で満点 |
| 15点 | 5日騰落率の絶対値 | 上下どちらでも「動いている」銘柄を評価。8%で満点 |

上昇・下落の方向は問いません。デイトレでは空売りも使えるため、
「どちらでもいいから動いている」ことを評価する設計です。

## 出力例

```
==============================================================================
 日経225 デイトレ候補  2026-08-01 (Fri) 08:45 JST  (2026-07-31 終値ベース)
==============================================================================
 # コード    銘柄                    終値     前日比    ATR     出来高比      代金    点
------------------------------------------------------------------------------
 1 6920   レーザーテック   22,561   -0.3%   5.9%     3.2x   984億   95
 2 9984   ソフトバンクG    12,873   +0.3%   5.3%     1.8x 1,557億   84
 3 7203   トヨタ自動車      3,523   +1.1%   2.3%     1.0x   712億   51
------------------------------------------------------------------------------
 1. 6920 レーザーテック: 出来高急増 / 高ボラ
 2. 9984 ソフトバンクG: 出来高増 / 高ボラ
 3. 7203 トヨタ自動車: —
```

`--out-dir` を指定すると、各銘柄の前日レンジ・想定変動幅（ATR）・20日高安値を
含んだMarkdownレポートも保存されます。

## 毎朝自動で実行する（GitHub Actions）

`.github/workflows/nikkei-daytrade.yml` を同梱しています。GitHubのサーバーが平日の朝に実行するため、
自分のPCの電源が入っている必要はありません。publicリポジトリなので無料枠の消費もありません。

### 設定手順

**1. ワークフローをデフォルトブランチに置く**

GitHubの仕様上、スケジュール実行は**デフォルトブランチにあるワークフローしか動きません**。
このブランチをデフォルトブランチにマージしてください。

**2. Gmailアプリパスワードを取得する**

通常のGmailパスワードでは送信できません。2段階認証を有効にしたうえで、
[Googleアカウントのアプリパスワード](https://myaccount.google.com/apppasswords) で16桁のパスワードを発行します。

**3. GitHub Secrets に登録する**

リポジトリの Settings → Secrets and variables → Actions → New repository secret で3つ登録します。

| Secret名 | 値 |
|----------|-----|
| `GMAIL_FROM` | 送信元のGmailアドレス |
| `GMAIL_TO` | 受信したいメールアドレス |
| `GMAIL_PASSWORD` | 手順2で発行した16桁のアプリパスワード |

Secretsは暗号化され、ログにも表示されません。publicリポジトリでも他人から読み取られることはありません。

**4. 動作確認する**

Actions タブ →「日経225 デイトレ候補」→ Run workflow で手動実行できます。
スケジュールを待たずにメールが届くか確認してください。

### 実行タイミング

平日 **8:20 JST**（UTC 23:20）に設定しています。8:45ではなく8:20にしているのは、
GitHubのスケジュール実行が混雑時に5〜20分ほど遅延することがあるためで、
寄り付き（9:00）までに確実にメールが届くようにするための余裕です。

早すぎる・遅すぎる場合は `.github/workflows/nikkei-daytrade.yml` の `cron` を調整してください。
**UTC指定**なので、JSTから9時間引いた値を書きます（曜日も前日にずれます）。

### 実行結果の保存

毎回のレポートは `reports/YYYY-MM-DD.md` としてリポジトリにも自動コミットされます。
過去の選定結果が残るので、後から「このスコアは当たっていたか」を検証できます。

### 注意事項

- **祝日も実行されます。** 日本の祝日判定は入れていないため、休場日には前営業日と同じ内容のメールが届きます
- **60日間リポジトリに動きがないと、GitHubはスケジュール実行を自動停止します。** 毎日のレポート自動コミットで動きは発生しますが、確実に防げる保証はありません。しばらくメールが来ないと感じたらActionsタブを確認してください
- **Yahoo Finance がクラウドのIPを制限する可能性があります。** 取得成功率が60%を下回るとワークフローは失敗し、GitHubから通知メールが届きます。頻発する場合は下記のPC実行に切り替えてください

## PCで自動実行する（GitHub Actionsを使わない場合）

### macOS / Linux（cron）

`crontab -e` で以下を追加します（平日のみ、8:45に実行）。

```cron
45 8 * * 1-5 cd /path/to/repo && /usr/bin/python3 nikkei_daytrade.py --out-dir reports >> screener.log 2>&1
```

cronはOSのローカルタイムで動くため、マシンのタイムゾーンがJSTになっていることを確認してください（`date` で確認）。

Gmail通知も使う場合は、パスワードをcronの行に直接書かず環境変数で渡してください。

```bash
GMAIL_FROM=you@gmail.com
GMAIL_TO=you@gmail.com
GMAIL_PASSWORD=xxxxxxxxxxxxxxxx
45 8 * * 1-5 cd /path/to/repo && /usr/bin/python3 nikkei_daytrade.py --out-dir reports >> screener.log 2>&1
```

`--gmail-from` などのオプションでも指定できますが、その場合は同じマシンの他ユーザーから
プロセス一覧（`ps`）でパスワードが見えてしまう点に注意してください。

### Windows（タスクスケジューラ）

1. タスクスケジューラを開き「基本タスクの作成」
2. トリガー: 毎日 8:45（詳細設定で平日のみに変更）
3. 操作: プログラムの開始
   - プログラム: `python`
   - 引数: `nikkei_daytrade.py --out-dir reports`
   - 開始（作業フォルダ）: リポジトリのパス

## 選定結果を検証する（答え合わせ）

`nikkei_verify.py` は、過去に選んだ銘柄で実際に売買していたらどうなったかを、実際の株価で計算します。
**このスコアに意味があるのかを、1円も使わずに確かめるための機能です。**

### 検証ルール

- 選定した**翌営業日の寄付で等金額ずつ買い、その日の引けで全部売る**
- 往復の手数料+スリッページとして 0.1% を差し引く（`--cost-pct` で変更可）
- **同じ日に日経平均を同じルールで売買した場合と比較する**

3つ目が重要です。相場全体が上がった日は何を買っても儲かるため、日経平均を上回っていなければ
「銘柄を選んだ意味がなかった」ことになります。

### 使い方

GitHubのActionsタブから **「選定結果の検証」→ Run workflow** で実行できます（PCは不要）。
結果は実行後のサマリ画面に表示されます。

ローカルで動かす場合:

```bash
python nikkei_verify.py            # reports/ の全レポートを検証
python nikkei_verify.py --top 3    # 各日の上位3銘柄だけ検証
python nikkei_verify.py --detail   # 1取引ずつ明細を表示
```

### 出力例

```
売買日             銘柄数      平均損益      勝率      日経平均        差
------------------------------------------------------------------------------
2026-08-03        10    +0.42%     60%    +0.31%   +0.11pt
2026-08-04        10    -0.88%     30%    -0.52%   -0.36pt
------------------------------------------------------------------------------

■ 全体
  取引数      : 20 件 (9勝 11敗)
  勝率        : 45.0%
  平均損益    : -0.230% / 取引

■ 日経平均を同じルールで売買した場合との比較
  日経平均    : -0.105% / 日
  差          : -0.125pt

■ 判定
  【判断保留】検証日数が 2 日しかありません。
  20日、できれば60日ためてから判断してください。
```

### 判定の見方

| 表示 | 意味 |
|------|------|
| 判断保留 | 検証日数が20日未満。数字は運の影響が大きく、良くても悪くても意味がない |
| 見送り | 日経平均に負けている、または平均損益がマイナス。銘柄選定に価値が見えない |
| 継続検証 | 日経平均を上回っている。ただし損切りルールが無い状態の数字である点に注意 |

### 検証の限界

以下は**計算に含まれていません**。実際の成績はこれより悪くなります。

- **寄付で必ず約定する前提**です。ストップ高・ストップ安や板が薄い場面では、この価格では買えません
- **損切りがありません**。引けまで持ち切る前提なので、途中でどれだけ含み損を抱えても耐えたことになっています（`--detail` の「最大損」列で実際の下げ幅を確認してください）
- **税金を引いていません**（利益の約20%）
- **サンプルが少ないうちは、勝っていても偶然です**。20日で20%勝ち越すようなことは、コインを投げていても普通に起こります

## 注意事項

- 株価データは Yahoo Finance の公開チャートAPIから取得しています。仕様変更で動かなくなる可能性があります
- 8:45時点では当日の板情報・気配値は取得できません。前営業日終値までの日足がベースです
- 決算発表やニュースによるギャップは日足データには織り込まれません。寄り付き前に別途確認してください
- 本ツールは機械的な抽出であり、投資判断を推奨するものではありません。売買は自己責任で行ってください

---

# エリオット3波入り候補スクリーナー

日経225構成銘柄の日足から、**1波の上昇 → 2波の押し**という形を機械的に検出し、
2波が終わって**3波に移行しようとしている銘柄**をスコア順に抽出します。
大引け後（15:30以降）に実行し、翌日以降のエントリー候補を絞り込む想定です。

エリオット波動では3波が最も伸びやすい波とされているため、
「2波の底で仕込み、1波高値の上抜けで3波に乗る」という狙い方の下準備にあたります。

## 使い方

```bash
# 上位10銘柄をターミナルに表示
python elliott_wave3.py

# 上位15銘柄、レポートを reports/elliott/ に保存
python elliott_wave3.py --top 15 --out-dir reports/elliott

# 1波が12%以上、2波の底から7日以内の銘柄だけに絞る
python elliott_wave3.py --min-wave1 12 --max-age 7
```

銘柄リストは日経225のスクリーナーと共通で `data/nikkei225.csv` を使います。
更新は `python nikkei_daytrade.py --update-list` で行えます。

### 波形の判定ルール

日足の高値・安値からジグザグ（転換点）を引き、直近が **安値 → 高値 → 安値** の並びになっている銘柄を探します。
そのうえで、次を満たすものだけを候補にします。

| 条件 | デフォルト | 理由 |
|------|-----------|------|
| 2波の底 > 1波の起点 | 必須 | **エリオットの絶対ルール**。1波の始点を割ったら、この数え方は成立しない |
| 1波の上昇率 | 8%以上 | 小さすぎる波はノイズと区別がつかない |
| 2波の押し戻し率 | 38.2〜78.6% | 2波は深く押すのが普通。38.2%未満の浅い押しは4波の形に近い |
| 2波の底からの経過 | 1〜15日 | 反発が始まっていて、かつ動き出したばかりであること |
| 現在値 | 2波の底より上、1波高値より下 | まだ高値を抜いていない＝「これから3波」の状態 |
| 2波の戻し | 下げ幅の70%まで | 戻しすぎた銘柄（もう半値戻し以上）は妙味が薄い |
| 転換点の閾値 | 4% または ATRの2.5倍 | 値動きの荒い銘柄ほど閾値を広げ、ノイズを転換点と誤認しない |

1波高値をすでに抜いた銘柄も見たい場合は `--allow-breakout` を付けてください。

### 波形の頑健性チェック（照合）

同じチャートでも、転換点の閾値を変えると波の数え方は変わります。そこで候補ごとに、
**閾値4%固定（チャートツールのエリオット指標に多い設定）でも同じ2波の底が出るか**を照合し、
レポートに `照合 ○ / −` として出しています。

- **○** … 閾値の取り方に依存しない波形。ノイズを1波2波と読んでいる可能性が低い（+10点）
- **−** … その閾値でしか見えない波形。細かい上下動を波として数えている可能性がある

照合に使う閾値は `--cross-swing` で変更できます（`0` で照合なし）。

### 業績チェック

**押し目に見えても、業績悪化の初動ならそのまま下抜けます。** そこで波形の条件を満たした銘柄だけ、
Yahoo Finance から業績を取得し、**実績（直近四半期）** と **来期予想** の2軸で評価します。

**実績** — 直近四半期の前年同期比です。

| 格付け | 条件 | スコア調整 |
|--------|------|-----------|
| ◎ 増収増益 | 利益成長 +10%以上 | +8 |
| ○ 増益 | 利益・売上ともにプラス | +3 |
| △ 減速 | どちらかがマイナス（利益は-10%以内） | -3 |
| ✕ 減収減益 | 利益成長 -10%以下、または増収も増益もなし | -12 |
| ? データなし | 成長率が取得できない | 0 |

**来期** — アナリスト予想EPSの「今期 → 来期」の増減率です。実績が失速していても来期回復予想なら
評価が変わり、逆に実績が良くても来期減益予想なら減点されます。

| 来期予想EPS | スコア調整 |
|------------|-----------|
| +10%以上 | +5 |
| 0%以上 | +2 |
| -10%まで | -3 |
| -10%より下 | -6 |

さらにレポートには、**前回決算がコンセンサスに対して上振れたか下振れたか**（実績EPS vs 予想EPS）と、
**直近4四半期で何回上振れたか**、**通期純利益の4年推移**を載せています。

> ⚠️ 「来期予想」は**アナリストのコンセンサス予想**であって、**会社が出すガイダンス（会社予想）ではありません。**
> Yahoo Finance からは日本企業の会社予想が取れないためです。保守的に出す会社もあれば強気な会社もあるので、
> 会社予想との差は決算短信で確認してください。

`--exclude-bad` を付けると実績 ✕ の銘柄を除外します（**自動実行では有効にしています**）。

**決算発表日**も取得し、7日以内なら警告を出します（`--earnings-warn` で変更可）。
決算をまたぐとギャップで損切り位置を飛ばされるため、2波の底に損切りを置く前提と相性が悪いためです。

業績データは `python yahoo_fundamentals.py 9021 1925` で単体でも確認できます。
`--no-fundamentals` を付ければ、波形だけの判定に戻せます。

### 需給チェック（信用残）

**信用買残が積み上がっている銘柄は、戻り売りで上値が重くなります。** そこで
日本証券金融（日証金）の貸借取引残高を日次で取得し、需給を評価します。
全銘柄分が1つのCSVにまとまっているため、追加のリクエストは1回だけです。

**貸借倍率 = 融資残高（買い方） ÷ 貸株残高（売り方）**

| 貸借倍率 | 状態 | スコア調整 |
|---------|------|-----------|
| 0.8倍未満 | 売り長（踏み上げ余地） | +4 |
| 0.8〜1.5倍 | 融資と貸株が均衡 | +1 |
| 1.5〜3倍 | やや買い長 | 0 |
| 3〜8倍 | 買い長 | -2 |
| 8〜15倍 | 買い長・上値が重い | -4 |
| 15倍超 | 大きく買い長・戻り売り圧力 | -6 |
| 貸株残ゼロ | 売り方不在 | -5 |

これに**買い残の増減**（残高比 -5%以下 +3 / マイナス +1 / 横ばい -1 / +5%以上 -3）と、
**貸借規制**（申込停止・注意喚起なら -3）を加えます。合計で **-12〜+7** の範囲です。

買い残が減っていれば「しこりの整理が進んでいる」、増えていれば「戻り待ちの売りが積み上がっている」
という読み方になります。`python jsf_margin.py 9021` で単体でも確認できます。

> ⚠️ 日証金の貸借残は、**証券会社が日証金から借りた分**であって、東証が公表する信用残そのものではありません。
> 一般信用や、日証金を通さない分は含まれません。方向感を日次で追う用途には十分ですが、
> 正確な信用買残は東証の週次公表（火曜・PDF）を確認してください。データは1営業日遅れです。

### スコアの内訳

**総合 = 波形スコア（100点満点） + 実績調整（-12〜+8） + 来期調整（-6〜+5） + 需給調整（-12〜+7）** です。
波形の質・業績・需給を混ぜず、レポートには内訳を並べて出します。

| 配点 | 指標 | 考え方 |
|------|------|--------|
| 25点 | 押し戻し率 | 50〜61.8%が最も2波らしい。浅すぎ・深すぎは減点 |
| 18点 | 2波の出来高減少 | 押し目で商いが細るのは、売り圧力が枯れているサイン |
| 20点 | 反発の勢い | 5日線回復（7点）／反発後の出来高増（7点）／直近3日の上昇（6点） |
| 15点 | 1波の力強さ | 上昇率（8点）とATR何個分動いたか（7点） |
| 12点 | 地合い | 25日線>75日線（5点）／終値>75日線（4点）／1波が直近安値から出ている（3点） |
| 10点 | 波形の頑健性 | 4%閾値でも同じ2波の底が出るか（上記の照合） |

### 出力例

```
 # コード    銘柄                    終値      1波     押し    経過   照合      底   実績      来期      貸借   決    波形    総合
----------------------------------------------------------------------------------------------------------------
 1 7532   パン・パシフィック・イン       926  +24.1%    58%    6日   ○  未確定   ◎  +6.4%    0.6倍    !    72    87
 2 2871   ニチレイ          2,188  +22.3%    74%    3日   −  未確定   ◎ +31.0%    0.1倍         65    79
 3 1925   大和ハウス工業     4,609  +20.8%    52%    6日   ○  未確定   ○ +14.8%   10.5倍         72    78
 4 9021   西日本旅客鉄道     2,920  +31.9%    58%    5日   −  未確定   ✕  -1.7%    9.2倍         69    47
----------------------------------------------------------------------------------------------------------------
 1. 7532 パン・パシフィック: ◎ 増収増益 (売上 +10.2% / 利益 +38.0%) / 貸借倍率 0.61倍（売り長・踏み上げ余地） / 買い残が大きく減少（整理進行）
 2. 2871 ニチレイ: ◎ 増収増益 (売上 +34.1% / 利益 +22.5%) / 貸借倍率 0.11倍（売り長・踏み上げ余地） / 買い残が大きく増加（しこり増）
 3. 1925 大和ハウス工業: ○ 増益 (売上 +9.0% / 利益 +9.0%) / 貸借倍率 10.5倍（買い長・上値が重い） / 買い残は微減
 4. 9021 西日本旅客鉄道: ✕ 減収減益 (売上 -0.6% / 利益 -17.9%) / 貸借倍率 9.2倍（買い長・上値が重い） / 買い残が大きく増加（しこり増）
```

決算列の `!` は発表が近いという警告です。4番目の銘柄は波形だけなら上位ですが、
減収減益（-12）・来期も減益予想（-3）・買い長で買い残増加（-7）が重なって
総合で22点引かれ、`--exclude-bad` を付ければ除外されます。

`--out-dir` を指定すると、銘柄ごとに以下を含むMarkdownレポートも保存されます。

- 1波・2波の日付と価格（どこからどこまでを何波と数えたか）
- **エントリー目安**: 1波高値の上抜け
- **損切り**: 2波の底の少し下（ここを割ると波動の数え方自体が無効になる）
- **目標**: 2波の底から1波の値幅の1.0倍 / 1.618倍
- リスクリワード比
- 使った閾値と、4%閾値との照合結果
- 業績（売上・利益の前年同期比、営業利益率、ROE、PER、PBR、配当利回り）
- 来期予想EPS（今期→来期）と、前回決算の上振れ/下振れ、通期純利益の4年推移
- アナリストの目標株価と現値比、次回決算日
- 需給（貸借倍率、融資残・貸株残とその増減、回転日数、貸借規制の有無）

### 「底: 未確定」の意味

2波の底からの反発が、まだ転換点の閾値（4%以上など）に届いていない状態です。
**このツールは、あえてこの段階の銘柄を拾います。**

チャートツールのエリオット指標の多くは、転換点を確定するのに「感度」分の足数を待つ設計です
（例: 感度13なら底から13本）。確定を待てば形は綺麗ですが、そのころには値も動いています。
本ツールは未確定の底を拾うことで早く気づける代わりに、
**そのまま下げ続けて「底ではなかった」で終わる候補が混じります**。

早さとダマシの少なさのトレードオフなので、慎重に見たい場合は
「照合 ○ かつ 底 確定」の銘柄だけを見てください。

### オプション

| オプション | デフォルト | 説明 |
|-----------|-----------|------|
| `--top` | 10 | 表示する上位銘柄数 |
| `--min-wave1` | 8 | 1波の最低上昇率（%） |
| `--min-retrace` / `--max-retrace` | 0.382 / 0.786 | 2波の押し戻し率の範囲 |
| `--max-age` | 15 | 2波の底からの最大経過日数 |
| `--max-recovery` | 0.70 | 2波の下げ幅をどこまで戻した銘柄まで拾うか |
| `--allow-breakout` | — | 1波高値をすでに抜いた銘柄も対象にする |
| `--min-rr` | 1.0 | リスクリワード（1.618目標）の下限 |
| `--swing` / `--swing-atr` | 4.0 / 2.5 | 転換点とみなす逆行率の下限（%）とATR倍率 |
| `--cross-swing` | 4.0 | 波形の照合に使う固定閾値（%）。0で照合なし |
| `--no-fundamentals` | — | 業績データを取得せず、波形だけで判定する |
| `--no-margin` | — | 貸借取引残高（需給）を取得しない |
| `--exclude-bad` | — | 減収減益（✕）の銘柄を除外する |
| `--earnings-warn` | 7 | 決算発表が何日以内なら警告するか |
| `--min-turnover` | 10 | 20日平均売買代金の下限（億円） |
| `--min-price` / `--max-price` | 300 / 0 | 株価の下限・上限（円、上限0で無制限） |
| `--out-dir` | なし | レポートの保存先。`YYYY-MM-DD.md` と `.json` で出力 |
| `--require-fresh` | — | 最新データが当日でなければ（休場日）何もせず終了する |
| `--workers` | 6 | 並列取得数 |
| `--codes-file` | `data/nikkei225.csv` | 銘柄リストCSV |

## 毎日自動で実行する（GitHub Actions）

`.github/workflows/elliott-wave3.yml` を同梱しています。**平日 17:00 JST**（UTC 8:00）に実行し、
結果をメールで送り、`reports/elliott/YYYY-MM-DD.md` としてリポジトリにコミットします。

自動実行では `--exclude-bad` を有効にしているため、**減収減益の銘柄はメールに載りません**。
業績が悪い銘柄も含めて見たい場合は、ワークフローからこのオプションを外してください。

設定はデイトレスクリーナーと共通です（`GMAIL_FROM` / `GMAIL_TO` / `GMAIL_PASSWORD` の3つのSecret）。
[設定手順](#設定手順) を参照してください。すでに設定済みなら、このワークフローの追加だけで動きます。

**スケジュール実行はデフォルトブランチにあるワークフローしか動きません。**
このブランチをデフォルトブランチにマージしてください。

デイトレスクリーナーと違い、日本の休場日には `--require-fresh` により何も送信されません
（当日の日足が存在しないため休場と判定します）。

## チャートツールのエリオット指標と結果が食い違うとき

TradingViewなどの自動カウント指標が「カウント不確定」と出している銘柄を、
本ツールが拾ってくることがあります。ほとんどは次の3つが原因です（優劣ではなく設定差です）。

**1. 転換点の閾値が違う**

指標側は「最小4%」のような固定値が多く、本ツールはATR×2.5（値動きの荒い銘柄ほど広い）です。
たとえば9021 西日本旅客鉄道（2026-08-14時点）はATR 2.87%なので閾値7.2%になり、次のように見え方が変わります。

| 閾値 | 直近の転換点 |
|------|-------------|
| 7.2%（本ツール） | 安値 6/04 2,453 → **高値 7/29 3,236** → **安値 8/06 2,782** |
| 4%（指標側） | 安値 7/22 2,893 → 高値 7/29 3,236 → 安値 8/06 2,782 → 高値 8/07 2,978 → 安値 8/13 2,834 |

4%では 8/07 と 8/13 の小さな上下動も転換点として拾うため、直近が「安値→高値→安値」の
綺麗な1-2波に見えず、カウントが確定しません。この食い違いは **照合 −** として表示されます。

**2. 確定待ちの遅延**

指標側は転換点の確定に感度分の足数を待ちます（感度13なら底から13本）。
底が5本前なら、指標側は構造的にまだ確定できません。本ツールは未確定の底を拾うので、その分早く出ます。

**3. 波の「格」の解釈**

本ツールは直近3つの転換点しか見ないため、**もっと大きな下降の途中の戻り**を1波と数えてしまうことがあります。
指標側が上位の波を追っていると、そもそも数えている波の階層が違います。これは本ツールの明確な弱点です。

指標側の設定に寄せたい場合は `--swing-atr 0 --swing 4` で閾値を4%固定にできます。

## 注意事項

- **エリオット波動のカウントは、そもそも一意に決まりません。** 同じチャートでも人によって数え方が変わります。本ツールの「1波・2波」はジグザグからの機械的な推定であり、正解ではありません
- **2波に見えて、実は下降トレンドの戻りだった、というケースが必ず混じります。** 損切り位置（2波の底）を守ることが前提の使い方になります
- **上昇波動（買い）だけを探します。** 下落の3波（売り）は検出しません
- **「来期」はアナリストのコンセンサス予想で、会社予想ではありません。** 会社が保守的な予想を出す銘柄では、会社予想とかけ離れることがあります
- **一過性の特益・特損は区別していません。** 「増益」に見えても資産売却益ということがあります。最終確認は決算短信で行ってください
- **需給は日証金の貸借残（1営業日遅れ）で見ています。** 東証の信用残（週次）とは対象範囲が違い、一般信用は含まれません。貸借銘柄でない銘柄は需給の判定を行いません（調整0）
- **日本株では成長率が欠損している銘柄があります**（`?` と表示）。この場合スコア調整は行いません
- 株価データは Yahoo Finance の公開チャートAPI、業績データは quoteSummary から取得しています。後者はクッキー認証が必要で、取得に失敗した場合は業績なしで続行します（レポートに `?` が並びます）
- 本ツールは機械的な抽出であり、投資判断を推奨するものではありません。売買は自己責任で行ってください

---

# メルカリ 価格アラートボット

指定したキーワードと上限価格でメルカリを定期監視し、新着出品をターミナルに表示します。

## 使い方

```bash
python mercari_alert.py <キーワード> <上限価格円> [--interval 秒]
```

### 例

```bash
# Nintendo Switch を ¥20,000 以下で60秒ごとに監視
python mercari_alert.py "Nintendo Switch" 20000

# AirPods Pro を ¥15,000 以下で2分ごとに監視
python mercari_alert.py "AirPods Pro" 15000 --interval 120
```

## 出力例

```
メルカリ監視開始
  キーワード : Nintendo Switch
  上限価格   : ¥20,000
  監視間隔   : 60秒
  停止       : Ctrl+C

初期データ取得中...
既存 30 件を確認。以降の新着を通知します。

============================================================
[14:35:22] 新着 2 件!  「Nintendo Switch」 ¥20,000以下
============================================================
  ¥ 18,000  Nintendo Switch 本体 ジョイコン付き
            https://jp.mercari.com/item/m12345678901
  ¥ 15,500  ニンテンドースイッチ 本体のみ
            https://jp.mercari.com/item/m98765432109
```

## 注意事項

- 監視間隔は最低10秒以上にしてください（サーバー負荷対策）
- Ctrl+C で停止できます

---

# 聖杯検証エンジン

「確実に勝てる手法（聖杯）」を数式で追い詰めるための検証ツールです。

結論を先に書くと、**確実に勝てる売買ルールは存在しません**。
これは市場が難しいからではなく、統計の構造からそう決まっています。
一方で「破産しないこと」と「まぐれと本物を見分けること」は数学的に保証できます。
このツールは、その境界がどこにあるかを実際に計算して示します。

```bash
# ネット接続不要のセクション（§2〜§5）をまとめて表示
python holy_grail.py

# reports/ に溜まった選定結果を統計検定する（要ネット）
python holy_grail.py --edge --trials 20

# 個別に実行する
python holy_grail.py --trap    # 多重検定の罠
python holy_grail.py --power   # 必要取引数の早見表
python holy_grail.py --risk    # 破産確率とケリー比率
python holy_grail.py --mix     # 分散によるシャープ改善
```

## §1 エッジ検定 `--edge`

`nikkei_verify.py` と同じ売買ルール（翌営業日の寄付で買い、引けで売る）で
`reports/` の選定結果を評価し、その成績が**まぐれで説明できるかどうか**を判定します。

`nikkei_verify.py` が「いくら儲かったか」を出すのに対し、こちらは
「その数字を信じてよいか」を出します。

| 指標 | 意味 |
|------|------|
| t値 / p値 | 平均リターンがゼロと区別できるか。t分布で厳密に計算 |
| 平均の95%区間 | ブートストラップ法。正規分布を仮定しないので裾が厚くても壊れない |
| まぐれの閾値 | 優位性ゼロでも、N回試せばこの程度のシャープは出てしまうという水準 |
| DSR | Deflated Sharpe Ratio。試行回数で割り引いた「本物である確率」。95%未満は採用不可 |
| 必要日数 | その優位性を95%の確度で証明するのに要るデータ量 |

`--trials` には**これまでに試した戦略・パラメータの総数**を渡します。
スコアの配点を変えて試した回数、途中で捨てた条件、その全部が試行回数です。
ここを正直に申告しないと DSR は意味を持ちません。

### 同じ日の銘柄は独立ではない

このセクションが最も強く警告する点です。1日に10銘柄選べば10取引ですが、
地合いが良ければ10銘柄とも上がるので、**独立した10個の観測ではありません**。

`n = 取引数` として t 値を計算すると、実力より大きな数字が出ます。
本ツールは1日1観測（等金額バスケットの日次損益）を正とし、
取引ベースで数えた場合の t 値も並べて、水増し倍率を表示します。

## §2 多重検定の罠 `--trap`

期待値がちょうどゼロの乱数から「聖杯」を捏造して見せます。

```
     試行数       最高ｼｬｰﾌﾟ(年率)       年率ﾘﾀｰﾝ       t値        p値      DSR
------------------------------------------------------------------------------
       1              0.72        11.5%     0.73    0.4673    76.4%
      10              1.65        23.4%     1.67    0.0961    46.2%
      50              1.80        27.0%     1.82    0.0699    47.1%
     200              2.61        42.5%     2.64    0.0088    46.6%
    1000              2.94        45.7%     2.97    0.0032    39.2%
```

中身は全部ただの乱数です。それでも200通り試せば、年率シャープ2.6・
p値0.009 の美しい右肩上がりの資産曲線が手に入ります。

注目すべきは一番右の DSR で、試行数がいくつでも **50%前後に張り付きます**。
「N本中の最良は、期待される最大値そのもの」という理論通りの値で、
コイン投げと区別がついていないことを意味します。p値が0.003でも、です。

> **聖杯の第一条件** — 試した回数を数え、それで割り引いた後にも残る優位性だけを信じる

## §3 検出力 `--power`

その優位性を証明するのに何取引必要か、という早見表です。

```
       1取引シャープ        年率シャープ        必要取引数        日1回なら        現実味
------------------------------------------------------------------------------
          0.02          0.31       19,622        80.1年       検証不能
          0.05          0.78        3,140        12.8年      一生かかる
          0.10          1.57          785         3.2年         数年
          0.20          3.13          196         0.8年        現実的
```

実在する優位性のほとんどは1取引シャープ 0.02〜0.05 の領域にあります。
つまり証明には数千取引が要ります。逆に「3ヶ月で確認できた優位性」は、
大きすぎて実在を疑うべき水準です。

> **聖杯の第二条件** — 取引頻度を上げる。1取引の優位性は増やせないが、回数は増やせる

## §4 資金管理 `--risk`

ここが本当の聖杯の在り処です。優位性の大きさは市場が決めますが、
賭け金の大きさは自分で決められます。唯一こちらが完全に支配できる変数です。

モンテカルロ4,000本で、賭け金だけを変えた結果を実測します
（1取引あたり平均+0.05%・標準偏差1.5%・2,450取引＝約10年）。

```
       賭け金   ﾚﾊﾞﾚｯｼﾞ       理論成長率       資産中央値       下位5%      最大DD中央    DD50%超      破産
------------------------------------------------------------------------------
 0.25x ｹﾘｰ     0.56x     0.0243%       1.83倍      0.93倍       26.5%      1.7%    0.0%
  0.5x ｹﾘｰ     1.11x     0.0417%       2.83倍      0.73倍       47.6%     42.2%    0.0%
    1x ｹﾘｰ     2.22x     0.0556%       4.04倍      0.27倍       76.3%     98.9%    1.1%
    2x ｹﾘｰ     4.44x     0.0000%       1.06倍      0.00倍       97.7%    100.0%   24.1%
    3x ｹﾘｰ     6.67x    -0.1667%       0.02倍      0.00倍      100.0%    100.0%   65.0%
```

**同じ優位性、同じ売買ルールです。違うのは賭け金だけ**です。
それでも10年後の資産は 1.83倍 にも 0.02倍 にもなります。

ケリー比率の2倍を賭けると成長率はちょうどゼロになり、それ以上では
期待値プラスの取引を繰り返しながら資産がゼロに収束します。
運ではなく、log の凹性から来る確定した帰結です。

ハーフケリーの成長率はフルケリーの75%ですが、最大ドローダウンは76%→48%に下がります。
成長を25%諦めるだけでリスクが半減する、この非対称性が実務上の答えです。

> **聖杯の第三条件** — ケリー比率の1/2以下で賭ける
>
> (1) μとσの推定値には必ず誤差があり f* を過大評価する
> (2) 実際のリターンは正規分布より裾が厚い
> (3) 途中のDDに人間が耐えられず最悪のタイミングで降りる

## §5 分散 `--mix`

同じ強さの優位性を k 本、相関 ρ で束ねた時の合成シャープです。

```
     相関ρ       k=1       k=2       k=3       k=5      k=10      k=20      k=50
------------------------------------------------------------------------------
     0.0      1.00      1.41      1.73      2.24      3.16      4.47      7.07
     0.3      1.00      1.24      1.37      1.51      1.64      1.73      1.78
     0.7      1.00      1.08      1.12      1.15      1.17      1.18      1.19
```

相関ゼロなら10本でシャープ3.16倍。元が0.3の使い物にならない優位性でも、
10本集めれば0.95になります。逆に相関0.9では10本束ねても1.05倍で、ほぼ無意味です。
「日本株の中で10戦略」は大体ここに入ります。全部同じ地合いに賭けているためです。

必要取引数はシャープの2乗に反比例するので、ρ=0でk本束ねると
**検証に必要な期間も 1/k になります**。分散は儲けだけでなく証明速度にも効きます。

> **聖杯の第四条件** — 相関の低い優位性を可能な限り多く並べる
>
> 1本の完璧な手法を探すのは、数学的に最も効率の悪い戦い方

## 聖杯の正体

確実に勝てるルールが見つかったなら、それは

- (a) 試行回数を数えていない（§2）か
- (b) サンプルが足りていない（§3）

のどちらかです。例外はありません。一方で次の4つは数学的に保証されています。

1. 試行回数で割り引いた後に残る優位性は、本物である
2. 同じ優位性でも、取引回数を増やせば年率シャープは伸びる
3. ケリー比率の半分以下で賭ければ、破産確率は実質消える
4. 相関の低い優位性を k 本束ねれば、シャープは √k 倍になる

聖杯とは単一の手法ではなく、この4つを回し続ける**手順**のことです。

このリポジトリで言えば、`nikkei_daytrade.py` / `elliott_wave3.py` が「候補を出す」係、
`nikkei_verify.py` が「実際いくらになったか数える」係、
`holy_grail.py` が「まぐれと本物を分ける」係にあたります。
足りていないのは資金管理の実装と、相関の低い2本目の優位性です。

## 実装について

標準ライブラリのみで動きます。scipy が無い環境でも Deflated Sharpe Ratio を
計算できるよう、正規分布の分位点（Acklam近似＋Halley法で補正）と
正則不完全ベータ関数（Lentz法の連分数展開）を自前で実装しています。
t分布のp値は数値積分と 1e-13 まで一致することを確認済みです。

参考: Bailey & López de Prado, *The Deflated Sharpe Ratio* (2014),
*The Probability of Backtest Overfitting* (2016)

---

# 資金管理・発注数量計算

`holy_grail.py --risk` は「ケリー比率の半分以下で賭けろ」で終わっています。
しかしそれは賭け金を無限に細かく刻める前提の話で、日本株には**単元株100株**という壁があります。
資金100万円では、この壁が理論より支配的になります。

```bash
# 100万円の設計図を全部出す（§1〜§4 + 結論）
python position_size.py

# 単元未満株（S株/かぶミニ/ワン株）で計算する
python position_size.py --unit 1 --risk 0.5

# reports/elliott の候補を実際の発注表にする
python position_size.py --order --unit 1 --risk 0.5 --invest-pct 73

# 1銘柄だけ計算する
python position_size.py --code 7532 --entry 984.5 --stop 866.4

# 資金を変える
python position_size.py --capital 5000000
```

## §1 単元株の壁 `--wall`

`reports/` に出た77銘柄で数えると、**1単元の中央値は461,000円**でした。
100万円だと2銘柄しか持てません。

| 判定 | 銘柄数 |
|------|--------|
| 1単元でも買える | 66 / 77 銘柄（86%） |
| 1単元が資金の20%以内（＝5銘柄に分散できる） | **10 / 77 銘柄（13%）** |

つまり100万円でまともに分散できるのは、株価2,000円以下の13%の銘柄だけです。
日経225の主力どころは1単元が資金の30〜50%を占めます。

## §2 現金比率 `--cash`

**100万円で最初に決めるべきはレバレッジではなく現金比率です。**

集中したポートフォリオはボラティリティが下がりません。`f* = μ/σ²` なので
σ が大きいほど賭けてよい金額は急激に減ります。

```
      保有銘柄数   ﾎﾟｰﾄﾌｫﾘｵﾎﾞﾗ      ｹﾘｰ比率     ﾊｰﾌｹﾘｰ         推奨投資額        現金
         1銘柄      2.50%      0.80x      0.40x      400,000円       60%
         2銘柄      2.17%      1.07x      0.53x      533,333円       47%
         5銘柄      1.94%      1.33x      0.67x      666,667円       33%
        10銘柄      1.85%      1.45x      0.73x      727,273円       27%
```

現物で持てる2銘柄の場合、ハーフケリーは0.53倍。
**100万円のうち約47万円は現金で置くのが正しい**という結論になります。
これは「余力を残す」という心構えの話ではなく、賭けすぎを避けるための計算結果です。

同じ100万円でも、2銘柄集中と10銘柄分散では賭けてよい金額が
533,333円と727,273円で1.4倍違います。**増やすべきは資金より銘柄数**です。

## §3 発注数量 `--size`

```
株数 = min( リスク許容額 ÷ 損切り幅 , 投資枠 ÷ 株価 ) を単元に切り下げ
```

制約は2つあり、小さい方が勝ちます。100万円・リスク1%・単元100株では:

```
      株価      損切り幅      理論株数      発注株数         投資額     実ﾘｽｸ      制約
    800円        5%       250      200株    160,000円    0.80%     リスク
  1,500円        5%       133      100株    150,000円    0.75%     リスク
  1,500円       10%        67      発注不可           —        —      単元
  2,500円        5%        80      発注不可           —        —      単元
  4,000円       10%        25      発注不可           —        —      単元
```

**株価1,500円を超えると、リスク1%では発注そのものができません。**
理論株数が100株に届かないためです。

## §4 連敗耐性 `--streak`

1トレードのリスクを定率にすると、負けるほど賭け金も減るため資産は幾何級数的にしか減りません。
これが「破産しない」の実装です。

```
   ﾘｽｸ/回          5連敗         10連敗         20連敗         30連敗           判定
    0.5%     975,249円    951,110円    904,610円    860,384円           余裕
    1.0%     950,990円    904,382円    817,907円    739,700円        耐えられる
    5.0%     773,781円    598,737円    358,486円    214,639円         再起不能
```

そして200取引の間に実際に起きる最大連敗数（モンテカルロ実測）:

```
      勝率        中央値      上位10%       上位1%       最悪
     40%         9連        13連        17連      30連
     50%         7連         9連        13連      19連
```

**勝率40%の手法なら10連敗は設計通りの挙動**であり、手法が壊れた証拠ではありません。
リスク1%なら10連敗しても資産は90%残ります。5%だと60%まで落ちて続けられなくなります。
差を作るのは勝率でも手法でもなく、1回あたりのリスクだけです。

## §5 発注表 `--order`

`reports/elliott/` の最新候補（`entry_trigger` と `stop` を持つ）を実際の株数に変換します。

**単元株100株・リスク1%の場合:**

```
  7532 パン・パシフ      984円      866円  12.0%        —    要1.2%
  6532 ベイカレント    7,728円    6,716円  13.1%        —   要10.1%
  9433 KDDI            3,121円    2,753円  11.8%        —    要3.7%
    合計  0銘柄                                      0円     0.00%
```

**10候補すべて発注不可**でした。ベイカレントは1単元772,800円で、
損切りに当たると資金の10.12%が飛びます。

**単元未満株・リスク0.5%の場合:**

```
  7532 パン・パシフ      984円      866円  12.0%      42株     41,349円   0.50%
  6532 ベイカレント    7,728円    6,716円  13.1%       4株     30,912円   0.40%
  9433 KDDI            3,121円    2,753円  11.8%      13株     40,573円   0.48%
    合計 10銘柄                                             381,940円   4.70%
```

10銘柄すべてに381,940円（資金の38%）で分散でき、全部損切りに当たっても損失は4.70%です。

## 100万円の運用設計（結論）

1. **現金比率を先に決める** — 投資枠50万円 / 現金50万円から始める
2. **単元未満株を使う** — 100万円で最も効く一手。手数料差より制約解除の価値が大きい
3. **1トレードのリスクは0.5〜1.0%** — 1回の損切りで5,000〜10,000円
4. **損切りの近い手法を選ぶ** — エリオット（損切り10%前後）より、デイトレ（2%前後）の方がこの資金量とは相性が良い
5. **増やすべきは資金ではなく銘柄数** — 2銘柄集中と10銘柄分散で賭けてよい額が1.4倍違う

> **注意** — 上の数字は全て「優位性が実在する」という前提の上に乗っています。
> `holy_grail.py --edge` の判定は現時点で【判断不能】（データ9日分）です。
> 資金管理はマイナスの期待値をプラスには変えられません。破産までの時間を延ばすだけです。
